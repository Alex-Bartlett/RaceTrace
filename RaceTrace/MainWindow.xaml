<Window x:Class="RaceTrace.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:oxy="http://oxyplot.org/wpf"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RaceTrace"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <Window.DataContext>
        <local:MainViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <TextBlock Name="txtFolderPath" Grid.Column="2" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="72" Text="{Binding Title, UpdateSourceTrigger=Explicit}"></TextBlock>
        <Button Width="200" Height="50" FontSize="28" Click="ChangeText">Click me</Button>
        <oxy:PlotView Model="{Binding MyModel}" Grid.Column="2" Grid.Row="2"/>
    </Grid>
</Window>
